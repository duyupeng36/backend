# Modern C 笔记：ch02-程序的基本结构

> 章节：第 2 章 · The Principal Structure of a Program（程序的基本结构）  
> 日期：2026-07-25  
> 状态：☑ 已完成

---

## 📋 速览

C 程序由**六种基本词法元素**（特殊词、标点、注释、字面量、标识符、运算符）按语法规则组合而成。理解 C 程序需要区分三个语义层面：

1. **声明** — 告诉编译器标识符代表什么（类型+名字）。所有标识符必须先声明后使用。声明有作用域（文件作用域、块作用域）。
2. **定义** — 声明 + 初始化器（或函数体）= 定义，创建实体并分配存储。每个对象/函数必须恰好有一个定义。
3. **语句** — 执行单元。三种基本类型：迭代（`for`）、函数调用（传值调用）、返回（`return`）。

关键区分：**声明**只描述，**定义**创建实体，**语句**执行动作。

---

## 📖 知识点


### 知识点 4：语句（Statements） ☑ 已完成


**是什么**

语句是 C 程序的基本执行单元，它告诉编译器如何处理已经声明和定义好的标识符

**详细解释**

示例程序 `getting-started.c` 的核心执行部分就是由三种类型的语句组成：**迭代语句**、**函数调用语句**和**返回语句**

```c
for (size_t i = 0; i < 5; ++i) {          // 迭代语句
    printf("element %zu is %g, \tits square is %g\n",
           i,
           A[i],
           A[i]*A[i]);                     // 函数调用语句
}
return EXIT_SUCCESS;                       // 返回语句

```

**迭代语句**：`for` 语句是 C 常用的范围迭代语句，由 $4$ 部分组成

```c
    //   (1)        (2)       (3)  (4)
for (size_t i = 0;  i < 5;    ++i)  {
    // 循环体
}

```

下表列出了 `for` 语句各个部分的执行时机和作用域

| 部分  | 名称     | 执行时机           | 作用                                 |
| ----- | -------- | ------------------ | ------------------------------------ |
| `(1)` | 初始化   | 循环开始前执行一次 | 声明并初始化循环变量 `i`             |
| `(2)` | 循环条件 | 每次迭代前检查     | `i < 5` 为真则执行循环体，为假则结束 |
| `(3)` | 迭代操作 | 每次迭代后执行     | `++i` 将 `i` 加 $1$                  |
| `(4)` | 循环体   | 每次条件为真时执行 | 打印结果                             |

`for` 语句的执行过程如下：`(1) → (2) → (4) → (3) → (2) → (4) → (3) → ...`

```
i = 0 → 检查 0 < 5? 真 → 打印 → ++i → i = 1
      → 检查 1 < 5? 真 → 打印 → ++i → i = 2
      → 检查 2 < 5? 真 → 打印 → ++i → i = 3
      → 检查 3 < 5? 真 → 打印 → ++i → i = 4
      → 检查 4 < 5? 真 → 打印 → ++i → i = 5
      → 检查 5 < 5? 假 → 结束
```

**函数调用**：挂起当前函数，把控制权交给被调用的函数，等它执行完 `return` 后，再回到原来的位置继续执行。例如示例程序 `getting-started.c` 中的 `printf(...)` 就是一个函数调用：`main() ──→ printf() ──→ return ──→ main() 继续`



> [!WARNING]
>
> C 程序中调用函数传递过去的是 **值** 而不是变量本身：`printf` 接收的 `i` 是值 $0$、`A[i]` 是值 `9.0` 、`A[i] * A[i]` 是值 `81.0`。换句话说，`printf` 中无法修改 `i` 和 `A[i]` 的值。这种机制称为 **传值调用**

**函数返回**：`return` 语句结束当前函数的执行，把控制权还给调用者。在 `main` 函数中

```c
return EXIT_SUCCESS; // 返回 EXIT_SUCCESS（通常定义为 0）
```

因为 `main` 的声明类型是 `int`，所以 `return` 必须返回一个 `int` 类型的值。``EXIT_SUCCESS` 表示"程序正常结束"。示例程序 `getting-started.c` 的整个执行流程是

```
系统启动例程 → 调用 main()
                ├── 声明 A 和 i
                ├── for 循环中调用 printf 5 次
                │    每次 printf 内部执行完 return 回来
                └── 执行 return EXIT_SUCCESS → 返回给启动例程 → 程序结束
```

**代码示例**

<!-- 这个小结没有示例代码  -->

**练习**

- [x] **练习 1**：for 循环执行过程 — 根据以下 `for` 语句，写出 `sum` 在每次迭代后的值：
  ```c
  int sum = 0;
  for (size_t i = 1; i <= 4; ++i) {
      sum = sum + i;
  }
  // 迭代1: i=1, sum=1
  // 迭代2: i=2, sum=3
  // 迭代3: i=3, sum=6
  // 迭代4: i=4, sum=10
  ```
- [x] **练习 2**：传值调用 — 以下代码中，`printf` 能否修改 `x` 的值？为什么？
  ```c
  int x = 42;
  printf("%d\n", x);
  ```
  答案：不能修改 `x` 的值，`printf` 中获取的 `x` 的值 42 而不是变量 x 本身
- [x] **练习 3**：完成 `getting-started.c` 的执行流程填空，标注每行代码的类型（声明、定义、迭代语句、函数调用、返回语句）

    | 类型                | 行号                        |
    | ------------------- | --------------------------- |
    | `main` 函数参数声明 | 第 4 行圆括号内容           |
    | `main` 函数定义     | 第 4 ~ 24 行                |
    | `A` 的定义          | 第 7 ~ 13 行                |
    | `i` 的定义          | 第 16 行 for 语句的第一部分 |
    | 迭代                |                             |

    

---

## 🤔 疑问记录

| 编号 | 疑问 | 状态 |
|------|------|------|
| 1 | 声明和定义的本质区别是什么？（答案：定义是扩展了的声明。定义本质上也是声明，只是需要为创建程序实体）"声明+初始化=定义"这条规则有例外吗？（比如函数：声明加分号是声明，加 `{}` 函数体才是定义）（答案：声明  + 初始化 = 定义说的数据对象，而不是函数） | ☑ 已解答 |
| 2 | 同一个标点 `{}` 在 C 中有三种不同用法（函数体、初始化列表、循环体），`[]` 也有三种（数组声明、下标、指定初始化）。这是 C 的设计特点还是历史负担？（答案：可以根据上下文推断符号的用途，不必为不同的用途设计不同的符号。而且，C 的声明是在模仿使用，去掉声明中的类型就是标识符的使用方式） | ☑ 已解答 |
| 3 | `main` 函数的参数 `argc` 和 `argv` 从哪里来？操作系统是如何把命令行参数传递给 C 程序的？（答案：调用该程序的进程传递过来的；通常操作系统，会计算参数的个数，并将参数打包在一个 `char*` 数组(最后一个元素必须是 `nullptr` 标记数组结束)中传递给程序） | ☑ 已解答 |

---

## ✅ 自检

- [ ] 能列出 C 程序的六种基本词法元素，理解"声明模仿使用"的设计原则（去掉类型就是使用方式）
- [ ] 能区分声明和定义："声明+初始化=定义"适用于数据对象，函数通过 `{}` 函数体来定义；能写出各种声明模式（`int x;` `int f(int);` `double A[5];`）
- [ ] 理解作用域的概念，能判断标识符属于文件作用域还是块作用域，知道同一标识符在不同作用域可以共存
- [ ] 能写出 `for` 循环四个组成部分的执行顺序（初始化→条件→体→更新），能跟踪每次迭代的变量变化；理解传值调用：函数接收的是变量的值而非变量本身
- [ ] 知道 `argv` 是 `nullptr` 结尾的字符串数组，`argc` 由操作系统计算后传入；理解指定初始化器 `[n]=value` 中未列出的元素默认为 0
