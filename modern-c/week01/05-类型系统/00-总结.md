# 05-类型系统 · 总结

> 16 个知识点：基本类型 → 语义类型 → 窄类型与整数提升 → 整数字面量 → 浮点字面量 → 字符和字符串字面量 → 隐式转换 → 初始化器 → 命名常量 → 复合字面量 → 无符号二进制表示 → 位运算符 → 移位运算符 → 有符号整数表示 → 定宽整数 → 精确定位宽整数

---

## 核心脉络

C 的类型系统分两层：**关键字提供的基本类型** + **头文件提供的语义类型**。

```
基本类型（关键字）
├── 整数：char / short / int / long / long long（各有 signed/unsigned）
├── 浮点：float / double / long double
└── bool（C23）

语义类型（<stdint.h>）
├── 定宽：int8_t / uint32_t / int64_t ...
└── 最小宽度：int_least8_t / int_fast32_t ...
```

## 三个关键认知

1. **C 标准只规定最小宽度，不规定精确宽度** — 用 `<stdint.h>` 的语义类型，别猜
2. **四种数据模型**（LP32/ILP32/LLP64/LP64）决定了 int/long/指针的实际宽度
3. **整数提升** — 小于 int 的类型运算时自动提升为 int

## 类型 Rank 等级

```
bool(0) < char(1) < short(2) < int(3) < long(4) < long long(5)
```

运算时，低 Rank 向高 Rank 转换。混合 signed/unsigned 时要小心。

## 位运算 = 集合运算

| 运算符 | 集合含义 | 示例 |
|--------|----------|------|
| `|` | 并集 | `A \| B` |
| `&` | 交集 | `A & B` |
| `^` | 对称差 | `A ^ B` |
| `~` | 补集 | `~A` |
| `<<` `>>` | 移位（乘/除 2 的幂） | `A << 3` |

## 易错点

- **混合 signed/unsigned 比较**：`-1 < 0U` 是 false（-1 转成 UINT_MAX）
- **窄化转换危险**：`unsigned short g = 0x80000000;` 截断为 0
- **有符号溢出是 UB**：`signed int c = 0x8000'0000;` 未定义行为
- **位运算操作的是位模式**，不是数值大小
- `~A` 和 `-A` 相差 1，原因是补码表示

## 教材练习（ch05）

### 基础练习

| # | 练习 |
|---|------|
| ch05-1 | 证明 `signed long long` 的最小值不能用字面量+负号写出:(`-(LLONG_MAX + 1)` 类型是 `unisgned long long`) |
| ch05-2 | 证明当最大 `unsigned` 为 $2^{16}-1$ 时，`-0x8000` 的值也是 $32768$:(`-x = (MAX + 1) - x mod (MAX + 1)`) |
| ch05-3 | 证明 `-1U`、`-1UL`、`-1ULL` 分别是三种无符号类型的最大值: （同上） |
| ch05-4 | 假设 `unsigned int` 最大值为 `0xFFFF'FFFF`，证明 `-0x8000'0000 == 0x8000'0000`: （同上） |
| ch05-5 | 证明 `A \ B` 可通过 `A - (A & B)` 计算: (`A \ B` 集合差集： 从 `A` 中移除 `A` 和 `B` 共同的元素) |
| ch05-6 | 证明 `V + 1` 为 0（`V` 是全 `1` 位模式）: (溢出丢弃最高位，剩下的全是 0) |
| ch05-7 | 证明 `A^B` 等价于 `(A-(A&B)) + (B-(A&B))` 和 `A+B-2*(A&B)`：（对称差集合推导） |
| ch05-8 | 证明 `A|B` 等价于 `A + B - (A&B)`：（并集的推导） |
| ch05-9 | 证明 `~B` 可通过 `V - B` 计算：（补集推导） |
| ch05-10 | 证明 `-B = ~B + 1`（补码原理） |
| ch05-11 | 证明 `x >> n` 丢失的位对应 `x % (1ULL << n)`:(右移丢弃的就是对 $2^{n}$ 取模的结果) |
| ch05-12 | 证明无符号算术中 `A + ~A` 是最大值：(A + A 的补集 = 全集) |
| ch05-13 | 证明无符号算术中 `A + ~A` 是 -1（即全 1）：(A + A 的补集 = 全集) |
| ch05-14 | 证明无符号算术中 `A + (~A + 1) == 0`：(A + A 的补集 = 全集； 全集 + 1 溢出回绕到 0) |
| ch05-15 | 证明 `INT_MIN + INT_MAX` 是 -1：(`INT_MIN = -2^{p-1}` `INT_MAX = 2^{p-1} - 1`) |
| ch05-16 | 思考固定宽度整数宏（`UINT8_WIDTH` 等）用 `N` 表示的封闭公式: `2^{N} - 1` |
| ch05-17 | 证明所有 `e > p` 的浮点值都是 $2^{e-p}$ 的倍数: `e: 指数宽度` `p: 精度宽度` |
| ch05-18 | 打印 `1.0E-13 + 1.0E-13` 和 `(1.0E-13 + (1.0E-13 + 1.0)) - 1.0` 的结果 |

### 挑战题

| # | 挑战 | 难度 | 状态 |
|---|------|------|------|
| ch05-C1 | 将数值导数（ch03-C2）扩展到复数域：接收和返回 `double complex` 的函数 | ⭐⭐ |

---

## 与下一周的衔接

第 2 周进入函数、数组与字符串——从"单个函数能做什么"扩展到"函数之间怎么传递数据"。
