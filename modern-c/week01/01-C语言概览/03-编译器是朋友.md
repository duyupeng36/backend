# 编译器是朋友

> week01 · C 语言全貌 · 知识点 3

> 状态：☑ 已完成

---

## 速览

编译器是程序员的朋友，不是敌人。它帮你检查代码中的错误（语法错误、类型错误、未定义行为）。启用所有警告（`-Wall -Wextra`）并认真对待每一条警告，是写好 C 代码的第一步。

---

## 是什么

编译器是程序员最严格的代码审查者。它会在编译阶段检查语法错误、类型错误和潜在问题。启用 `-Wall -Wextra -Werror` 编译选项后，编译器会将所有警告视为错误，强制程序员修复每一个潜在问题。把编译器当朋友，而不是敌人。一个好的 C 程序应该 **零警告** 编译通过

---

## 详细解释

《Modern C》 给出了一个有 bug 的版本（`bad.c`）在代码示例中，它与正确版本对比：至少有下面 $3$ 个问题

| 问题 | 错误写法 | 编译器反应 |
|------|---------|-----------|
| `main` 返回类型 | `void main()` | `error: return type of 'main' is not 'int'` |
| 缺少头文件 | 没有 `#include <stdio.h>` | `error: implicit declaration of function ‘printf’` |
| 返回语句矛盾 | `void` 函数里写 `return 0` | `error: ‘return’ with a value, in function returning void` |

GCC 用 `-Werror` 可以让 GCC 也变严格，直接把这些当错误，拒绝生成（更安全）可执行文件。

---

## 代码示例

```c
/* This may look like nonsense, but really is -*- mode: C -*- */

/* The main thing that this program does. */
void main() {              // ← 错误：main 应该返回 int
  int i;
  double A[5] = {
      9.0,
      2.9,
      3.E+25,
      .00007,
  };

  for (i = 0; i < 5; ++i) {
     printf("element %d is %g, \tits square is %g\n",  // ← 缺少 #include <stdio.h>
            i,
            A[i],
            A[i]*A[i]);
  }

  return 0;               // ← void 函数不应该 return 值
}
```

